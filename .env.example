# ホストの PulseAudio/PipeWire-pulse ソケットの実パス。
# `pactl info` 等で対象 Ubuntu ホストのランタイムディレクトリを確認してから設定すること
# （docs/catchup/docker_deployment_survey.md 5章参照）
PULSE_SOCKET_PATH=/run/user/1000/pulse/native

# コンテナ内プロセスの実行UID/GID。ホストの音声出力ユーザー（上記ソケットの所有者）と一致させる
PUID=1000
PGID=1000
